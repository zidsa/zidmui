# Tasks

- [ ] Fix theme type augmentations not included when package is installed — rename `types.d.ts` to `types.ts`, import it from theme files, update vite config, and verify augmented types resolve in consuming projects
